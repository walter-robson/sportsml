# sportsml.sports.baseball.mlb — stub

MLB plugin scaffold. Will provide Statcast-flavored ontology (`PitchEvent`, `BattedBall`, `AtBat`) and model templates (`xwOBA`, `pitch_quality_projection`). Not wired in v0.
