# sportsml.sports.football.nfl — stub

NFL plugin scaffold. Will provide:

- Source assets pulling from a paid feed (nflverse / SportRadar / etc.).
- Ontology extensions: `Play`, `Drive`, `Snap`, `PassingChart`.
- Model templates: `epa_per_play`, `passing_projection`, etc.

Not wired in v0.
